apiVersion: v1
kind: Service
metadata:
  name: locust-service
  namespace: locust
spec:
  selector:
    app: locust
  ports:
    - protocol: TCP
      port: 80 #8089
      targetPort: 8089
  type: ClusterIP #NodePort
